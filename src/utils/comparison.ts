export function compareDouble(x: number, y: number) {
  // const diff = Math.abs(x - y);
  // console.log('diff', diff, x, y, Number.EPSILON)
  // if (diff < Number.EPSILON) {
  //   return 0;
  // }

  return x - y;
}