{
  "name": "nodes2ts",
  "version": "1.1.1",
  "description": "",
  "main": "dist/export.js",
  "scripts": {
    "test": "NODE_ENV=test mocha",
    "wtest": "NODE_ENV=test nodemon -q -e ts --exec npm run test",
    "cover": "NODE_ENV=test nyc npm t",
    "//prepuablish": "shx rm -rf ./dist/ && npm run build",
    "clean": "shx rm -rf ./dist",
    "build": "npm run clean && tsc -d",
    "//postinstall": "npm run prepublish"
  },
  "typings": "./dist/export.d.ts",
  "author": "Andrea Baccega",
  "license": "MIT",
  "dependencies": {
    "decimal.js": "^6.0.0",
    "long": "^3.2.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "gulp": "^3.9.1",
    "gulp-spawn-mocha": "^3.0.0",
    "gulp-watch": "^4.3.9",
    "karma": "^1.1.2",
    "mocha": "^3.0.1",
    "nodemon": "^1.10.0",
    "nyc": "^7.1.0",
    "shx": "^0.1.2",
    "sinon": "^1.17.5",
    "ts-node": "^1.2.2",
    "typescript": "^1.8.10"
  },
  "nyc": {
    "include": [
      "src/*.ts",
      "src/**/*.ts"
    ],
    "exclude": [
      "typings"
    ],
    "extension": [
      ".ts"
    ],
    "require": [
      "ts-node/register"
    ],
    "reporter": [
      "html"
    ],
    "all": false
  }
}
